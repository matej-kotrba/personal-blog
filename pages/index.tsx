import type { NextPage } from "next";
import styled from "@emotion/styled";

const Home: NextPage = () => {
  return (
    <main className="container mx-auto mt-[100px] grid grid-cols-6 p-6 rounded-lg"></main>
  );
};

export default Home;
